import { describe, it, expect } from 'vitest'

// Placeholder — tests réels ajoutés dans les tâches 2, 3, 4
describe('specialties API', () => {
  it('placeholder — will be replaced in later tasks', () => {
    expect(true).toBe(true)
  })
})
